# Copy to .env.local (not committed).

# Optional: browser calls this origin directly (requires CORS on tmpvault-backend).
# Leave unset to use Next.js rewrites to TMPVAULT_BACKEND_URL instead.
# NEXT_PUBLIC_TMPVAULT_API_URL=http://127.0.0.1:8080

# Server-side only: proxy target when NEXT_PUBLIC_TMPVAULT_API_URL is unset.
# TMPVAULT_BACKEND_URL=http://127.0.0.1:8080
